#include "method.hpp"

int main(int argc, char *argv[])
{
    double h, alpha, p1_0, p2_0, B_0;
    double error;
    Point *head = (Point *)malloc(sizeof(head));

    printf("Введите параметр a: ");
    scanf("%lf", &alpha);


    clear_list(&head);
    return 0;
}